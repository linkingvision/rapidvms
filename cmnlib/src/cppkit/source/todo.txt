
cppkit todo

    bitsy
    cache
    easy weak ref
    ssl sockets
    statistics
    stop watch

lib ideas

    river - library for implementing rtsp client and servers!

    sdbl - A simple database layer.
        - SQLite & Postgresql backends
            - an alternative: supporty 1 SQL backend (postgres) and 1 KV backend (leveldb)
        - integrate tightly with cppkit types (list<map<ck_variant>> is result type).

    reason - library implementing collective intelligence ideas.
        Collabartive Filtering
          - Pearson correlation coefficient
          - Euclidean distance
        kmeans clustering
        quality threshold clustering
        bayes classifier

    ffkit - a port of ffkit to cppkit et al (probably just a bunch of renaming).
        - include new inside out pipeline framework

